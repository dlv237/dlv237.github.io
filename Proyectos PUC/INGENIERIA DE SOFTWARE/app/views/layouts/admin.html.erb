<!DOCTYPE html>
<html>
    <head>
        <%= stylesheet_link_tag "bulma", "data-turbo-track": "reload" %>
        <%= javascript_include_tag "turbo", type: "module" %>
    </head>
    <!-- header -->
    <nav class="navbar is-light" role="navigation" aria-label="main navigation">
    <div class="navbar-brand">
        <a class="navbar-item" href="/admin">
        Panel Admin
        </a>
    </div>

    <div id="navbarBasicExample" class="navbar-menu">
        <div class="navbar-end">
        <div class="navbar-item">
            <%= button_to 'Cerrar Sesión', destroy_admin_session_path, method: :delete, class: 'button is-light' %>
        </div>
        </div>
    </div>
    </nav>

    <!-- Pagina -->
    <main>
        <%= yield %>
    </main>

        <!-- mensajes flash con Bulma -->
    <% flash.each do |message_type, message| %>
        <div class="notification <%= bulma_class_for(message_type) %>">
        <%= message %>
        </div>
    <% end %>

<!-- footer -->

</html>